{
  "name": "My Multi-Repo Codespace",
  "customizations": {
    "codespaces": {
      "repositories": {
        "AaryanTheCoder/*": {
          "permissions": {
            "contents": "write"
          }
        }
      }
    }
  },
  "postCreateCommand": "mkdir -p /workspaces && cd /workspaces && gh repo list AaryanTheCoder --limit 100 --no-archived | awk '{print $1}' | grep -v 'Trading-the-stock-market' | xargs -I {} gh repo clone {}"
}
